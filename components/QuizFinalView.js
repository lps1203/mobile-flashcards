import React from 'react'
import { Text } from 'react-native'
import { connect } from 'react-redux'

function QuizFinalView(props) {
  return (
    <Text>QuizFinalView</Text>
  )
}

export default connect()(QuizFinalView)